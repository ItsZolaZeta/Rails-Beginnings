<% if(pin.errors.any?) %>   
    <div class="text-danger">
        <%= safe_join(pin.errors.full_messages, raw("<br />")) %>
    </div>
<% end %>  
<br>
<%= form_for(pin) do |form| %>
  <div class="form-group">
    <%= form.label(:title) %>
    <%= form.text_field(:title, class: 'form-control', placeholder: "E.g. Ride a camel") %>
  </div>

  <div class="form-group">
    <%= form.label(:image_url) %>
    <%= form.text_field(:image_url, class: 'form-control') %>
  </div>

  <div class="form-group">
    <%= form.label(:tag) %>
    <%= form.text_field(:tag, class: 'form-control') %>
  </div>

  <div class="prompt">
    <a class="prompt__cancel" href="#cancel">Cancel</a>
    <%= form.submit(class: 'btn btn-primary') %>
  </div>
<% end %>